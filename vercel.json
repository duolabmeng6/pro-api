{
    "builds": [
      {"src": "/app/main.py", "use": "@vercel/python"}
    ],
    "routes": [
      {"src": "/(.*)", "dest": "app/main.py"}
    ],
    "env": {
        "config_url": "config_url",
        "secret_key": "secret_key"
    }
}